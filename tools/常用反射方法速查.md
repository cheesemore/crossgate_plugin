# 常用反射方法速查（魔力宝贝：序章）

> 用途：DLL（`SeqChapter*.dll`，部署为 `hotfixdata/*.dll.bytes`）内通过反射调用游戏 API 的常用方法。
> 每项标注实现状态：**✅ 已实现**（现有代码可用）/ **⚠️ 协议已确认未封装**（只有协议档案）。
> 通用反射辅助见各 DLL 内的 `GetManagerInstance` / `FindType` / `GetMember` / `SetMember` / `CallMethod`。

---

## 1. 查询背包内某个格子的道具名 ✅ 已实现

**入口**：`PlayerDataHolder.GetItemDatasFromUid(string uid)` → `List<Com_Item>`（静态，含 Public/NonPublic）。

- 背包格下标：身上装备 0–7，背包 8–67（60 格）。
- 每个 `Com_Item`：`useFlag`（1=有道具）、`data`（`Proto_ItemData`）。
- 道具名取法两种：
  1. `data.Name`（`Proto_ItemData.Name` 字段，反编译 `hotfix.decompiled.cs:49440` 确认）—— 直接读格子道具名；
  2. `data.Id` → `ConfigManager.GetTbItemConfig().GetOrDefault(itemId).Name`（`ItemConfig.Name`，反编译 `502876` 确认）—— 按道具 ID 读配置名。

**现有代码参考**：
- `SeqChapterAutoStall.cs::GetItemDatas(uid)` + `CollectCandidates()`（读背包 [8..67]）
- `SeqChapterTestUi.cs::CheckEquippedMelee`（`GetMember(data, "Name")` 直接读格子道具名）
- `SeqChapterHelperBridge.cs::TryCollectSealCards`（读背包 + `useFlag` 过滤）

```csharp
var items = FindType("PlayerDataHolder")?.GetMethod(
    "GetItemDatasFromUid", BindingFlags.Public | BindingFlags.Static | BindingFlags.NonPublic)
    ?.Invoke(null, new object[] { uid }) as IList;
var item = items[bagIndex];                       // Com_Item
var data = GetMember(item, "data");               // Proto_ItemData
var name = Convert.ToString(GetMember(data, "Name") ?? "") ?? "";
```

---

## 2. 丢弃某个道具 ⚠️ 协议已确认，代码未封装

**游戏侧做法**（反编译 `hotfix.decompiled.cs:30895-30903, 329289`）：

```csharp
Manager<ItemManager>.Instance.SendBackPackMessage("丢弃道具", index, num, uid);
```

- 发送 `LSSPROTO_BACKPACK_FUNC` = **2001**，`Proto_CS_Backpack`
- 字段：`Type="丢弃道具"`，`Haveitemindex=index`（背包格下标），`Num=num`（数量），`KUid=uid`
- 入口源头：背包操作面板 `ItemOperatePanel.DropItem(num, info)`（`329289`）→ `SendBackPackMessage`

**DLL 反射封装**（未封装，需要时照此写）：

```csharp
var itemMgr = GetManagerInstance("ItemManager");
var m = itemMgr?.GetType().GetMethod("SendBackPackMessage",
    BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic,
    null, new[] { typeof(string), typeof(int), typeof(int), typeof(string) }, null);
m?.Invoke(itemMgr, new object[] { "丢弃道具", bagIndex, num, uid });
```

> 注意：这是「背包操作」类丢弃（回收站式），不是丢到地上。丢地上是 `Proto_CS_DropItem`（`LSSPROTO_DROP_ITEM_FUNC`=18，带 X/Y 坐标）。

---

## 3. 接收 NPC 更新（读缓存）✅ 已实现；服务端确实主动广播

### 结论
服务端会主动广播 NPC/角色/地图对象更新，客户端维护在
`EntityDataHolder.characterDatas`（`Dictionary<int, CharacterData>`，objindex → 数据）。
DLL 轮询读该缓存即可感知 NPC **出现 / 移动 / 消失**（含地图上突然刷出的 NPC）。

### 广播协议（服务端 → 客户端，客户端已有回调自动写缓存）
| 协议 | 编号 | 回调 | 作用 |
|---|---|---|---|
| `LSSPROTO_UPDATE_OBJ_FUNC` | 29 | `OnUpdateObjCallback` | NPC/角色出现、移动、属性变化 → 写/刷 `characterDatas`（`RefreshCharacterXY`） |
| `LSSPROTO_CLEAR_OBJ_FUNC` | 32 | `OnClearObjCallback` | NPC/角色消失 → `EntityContainer.Remove` → 清 `characterDatas` |
| `LSSPROTO_OBJ_ADDITIONAL_FUNC` | 31 | `OnObjAdditionalCallback` | 附加动作（走/跑/切换动作） |
| `LSSPROTO_MAP_FUNC` | 3 | `OnMapCallback` → `lssproto_M_recv` | 进图全量推地图 + NPC 实体 |
| `LSSPROTO_MAP_UPDATE_FUNC` | 3035 | `onMapUpdateCallBack` | 地图格/tile/object/event 增量更新 |

> 依据：反编译 `hotfix.decompiled.cs`（28130-28132 Register 三个 OBJ 回调；311721 OnMapCallback；311792 onMapUpdateCallBack）。

### DLL 读缓存方式（已验证，见 `SeqChapterHelperBridge.cs::ScanNpcsToCache`）

```csharp
var dict = GetStaticProperty("EntityDataHolder", "characterDatas") as IDictionary;
// 兜底：GetStaticField("EntityDataHolder", "<characterDatas>k__BackingField")
foreach (DictionaryEntry e in dict) {
    var cd = e.Value;                       // CharacterData
    var objindex = Convert.ToInt32(GetMember(cd, "objindex"));
    var name = Convert.ToString(GetMember(cd, "name"));
    var x = Convert.ToInt32(GetMember(cd, "x"));
    var y = Convert.ToInt32(GetMember(cd, "y"));
    var npcindex = Convert.ToInt32(GetMember(cd, "npcindex"));  // != -1 表示是 NPC
    var charEntityType = GetMember(cd, "charEntityType");       // CharacterEntityType
}
```

- 判断是不是 NPC：`npcindex != -1`（`EntityContainer.Remove` 里就是用它区分 NPC，见 `301128-301135`）。
- 注意：DLL 运行时**不要**自己 `NetManager.Register` 注册网络回调（现有 DLL 无此先例，注册时机/生命周期易出问题）；统一用「读缓存」方式轮询。

---

## 4. 游戏内 Tip 飘字 ✅ 已实现（首选反馈）

```csharp
var notify = GetManagerInstance("NotifyManager");
notify.GetType().GetMethod("Tip", new[] { typeof(string), typeof(bool) })
    ?.Invoke(notify, new object[] { "自动抓宠已开启", false });
```

参考：`SeqChapterAutoStall` / `SeqChapterTestUi` / 规则 `notify-tip-feedback`。

---

## 5. 使用道具 ✅ 已实现

```csharp
// ItemManager.SendUseItem(int haveitemindex, string uid) 或等价反射
// 发包 Proto_CS_UseItem / LSSPROTO_USE_ITEM_FUNC
var itemMgr = GetManagerInstance("ItemManager");
var m = itemMgr?.GetType().GetMethod("SendUseItem",
    BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
m?.Invoke(itemMgr, new object[] { bagIndex, uid });
```

参考：`SeqChapterTestUi.cs::TryUseMemoryItem`（龙族记忆；任务 119 破障石/寻兔符匹配 `Name` 或 `Secretname`）。

**赤凤之翼（Id=16337，Secretname「赤凤之翼」）是分页窗，不是用完即走：**

- `SendUseItem` 之后服务端下发 `LSSPROTO_WINDOWS` → `UI_WMDataBundle.buttonType` 按 `|` 切成 `value|name|value|name`
- 按钮同时带 **下一步**（`WindowButtonType.Next=0x20`）和 **确定**（Ok=1）
- 自动对话若把「确定+取消」当成输入框捷径、或数组里先扫到确定，会提前关窗
- 正确顺序：有「哥拉尔」选项先点；否则反复点下一步，没有下一步再点确定；弹窗关完再点任务

参考：`SeqChapterTestUi.cs` 步骤 2 `_escortUseItemPending` + `PickDialogueButton`。脚本页「测试赤凤之翼」走同一套点窗，弹窗关完 Tip「赤凤之翼使用成功」。

---

## 6. 是否在战斗 ✅ 已实现

```csharp
var inBattle = Convert.ToBoolean(GetStaticMember("BattleDataHolder", "IsInBattle") ?? false);
```

参考：`SeqChapterBearSlayer` / `SeqChapterTestUi` / `SeqChapterCountFarm`。

---

## 7. 寻路 / 导航 ✅ 已实现

优先用导航模块（可跨图）：

```csharp
// WalkSystem / TaskMove 相关：GeneralPointMoveTo(MapPoint) 或现有桥接封装
// 刷熊男等同图：WalkSystem.MoveTo(Vector2Int) —— 注意带地图号时用导航版
```

参考：`SeqChapterHelperBridge` 导航、`SeqChapterBearSlayer` 移动、`SeqChapterTestUi` 护航点任务。

传送落地 / 官方导航往回城走时，**不要**再 `GeneralPointMoveTo` 自己跳下一张；见第 17 节。

---

## 8. 重置 / 放弃任务 ✅ 已实现

```csharp
// Proto_CS_ResetTask，Type="重置任务"，Id=MissionConfig.RestartId，KUid=uid
// opcode LSSPROTO_RESET_TASK_FUNC = 1009
```

参考：`SeqChapterTestUi.cs` 龙族循环 `ResetDragon4ForAll`。

---

## 9. 银行存宠物 ✅ 已实现

```csharp
// ItemManager / Bank：SendBankMessage(BANK_TYPE.PERSONAL_BANK, "存宠物", ...)
// 先远程开个人银行再存；参考捉宠存包
```

参考：`SeqChapterAutoCatch.cs::StoreCaptainRestLevelOnePets`、`SeqChapterTestUi.cs::StoreLevelOnePetsForFull`。

---

## 10. 摊位：状态 / 寻摊 / 上架 ✅ 已实现

- 寻自己的摊：协议「查找摊位 / 寻找摊位」（**不是** `ActivityManager.SendActivityXY`）
- 上架：读 `seqchapter_auto_sell_prices.txt`，只上架表内装备
- 参考：`SeqChapterAutoStall.cs` 全流程状态机；研究笔记 `tools/单角色一键上架流程研究.md`

---

## 11. 改宠物名 ✅ 已实现

```csharp
// NetManager / Pet：SendChangePetName(...)
// 命名规则与捉宠一致（档/满/随机后缀）
```

参考：`SeqChapterAutoCatch` 命名、`SeqChapterTestUi` 一键命名。

---

## 约定（写新功能时）

1. **先搜本文 + 上表「参考」文件**，复制已通的反射片段。
2. 协议中文字符串必须与游戏一致（丢弃=`"丢弃道具"`）。
3. 开关类反馈优先 `Tip`，不要只改按钮文案。
4. 新确认的协议补进本文，避免下次再嗅探。

---

## 12. 任务口令对话（输入框+发送/取消） ✅ 已实现

自动任务走到 NPC 且 `scriptData.codePhrase` 非空时，客户端打开 **`NPCChatPanel`**（不是 `UI_WindowsMessage`）：

- 输入框：`m_ITxt_Message`（已填口令）
- 按钮：`m_Btn_Send` / `m_Btn_Cancel`
- 发送：`NPCChatPanel.SendMessage()` → `ChatManager.SendTalk(text, objIndex)` → `LSSPROTO_TALK_FUNC`

**现有代码**：`SeqChapterTestUi.cs::TryClickNpcChatPanelSend`（护航 `TryAutoPickDialogue` 优先点此）。

普通 NPC 选项窗仍走 `UI_WindowsMessage` + `NpcManager.SendWindows`。

---

## 13. 护航遇敌获取道具 ✅ 已实现

步骤文案含 **「遇敌」** / Hint **「开启原地遇敌」**，或主描述为击败/击杀/打倒并「获得」道具（排除纯交谈）时：

1. 有寻路点：先导航到 `movePoint[0]`（倒序表头=指定坐标，与侧栏点任务一致），**到位后**才 `SendAutoBattle("开始挂机", uid)`；未到点绝不开遇敌
2. 无寻路点：原地挂机，不因「无寻路点」暂停
3. 退战检查背包是否已有目标道具（文案「获得XXX」）或步骤已推进 → `SendAutoBattle("停止挂机", uid)` 后继续点任务

参考：`SeqChapterTestUi.cs` 护航 `EnterEscortEncounterWait` / `IsAtEscortEncounterDest`；协议字符串必须是 **「开始挂机」/「停止挂机」**。

---

## 14. 回登入点 ✅ 已实现

右下角 **更多 → 回登入点**（`CommonlyPanel` 按钮 `m_Btn_Record`，index **4**）。

确认框「确定要返回登入点吗？」之后：

```csharp
// 战斗中 Tip「战斗中暂时无法回记录点哟」并 return
// 组队非队长 Tip「队员状态下无法操作哟」
Manager<LoginManager>.Instance.SendLoginGate();
// LSSPROTO_LOGINGATE_FUNC = 60，Proto_CS_Logingate 空包
```

**不是**左下角「回法兰城」（`Com_ChatMini.m_Btn_Record` → `SendMenu(3,"-1")`）。

参考：`SeqChapterTestUi.cs::TrySendEscortLoginGate`（七夕 #119：护航页「七夕阿凯版/哥拉尔版」；步骤 2 开始时队长回登入点一次。**打完 boss 时常在战斗中就切步，回登入点发不出，出战后 tick 补发**；战斗中先 `AbortEscortTaskPathFully`，不要点任务/走卡死。落地后再彻底取消并等约 2 秒。步骤 5 挑战暗影巡卫改走挂机传送布朗山，不再回登入点。一轮以存兑换券为准。临时活动）。

---

## 15. 挂机导航「传送前往」✅ 已实现

挂机面板「前往」打了「前往→传送」补丁后，实际走：

```csharp
Manager<TaskManager>.Instance.SendMisc(navId);
// Proto_CS_Misc.Type = "挂机传送"，Id = navId
// LSSPROTO_MISC_FUNC = 78
```

`navId` 来自 `battle_tbautobattlenavigationconfig.Id`（不是 WayId）。例：**奇怪的洞窟怪 = 2**，**布朗山 = 6**（落点 floor=52709）。

客户端 UI 另有月卡/等级/前置任务/TransmitCount 冷却检查；DLL 直接 `SendMisc` 会跳过这些客户端检查（服务端仍可能校验）。

参考：`SeqChapterTestUi.cs::TrySendHangupTeleport`（七夕 #119 步骤 7 调查星月落痕·礁石开始前传送奇怪的洞窟怪；步骤 5 挑战暗影巡卫开始前传送布朗山，到图后再点任务导航）。

---

## 16. 账号银行存道具 ✅ 已实现

```csharp
Manager<ActivityManager>.Instance.SendActivity("远程账号道具仓库", uid, 0, 19);
Manager<RoleManager>.Instance.SendBankMessage(BANK_TYPE.ACCOUNT_BANK, uid, "存道具", 0, 0, indexList);
// indexList 为 Proto_ItemData.Index（不是背包格下标）
// LSSPROTO_ACCOUNT_BANK_FUNC = 2003；战斗中客户端会 Tip「战斗中不能存道具」
```

道具名匹配 `Name` 或 `Secretname`。七夕礼盒兑换券配置名是 Secretname「七夕礼盒兑换券」（Name 为「兑换券？」）。

参考：`SeqChapterTestUi.cs::StoreBagItemsToAccountBank`（七夕循环最后一步：停导航、**逐账号**存券并复查背包已空，账号间隔 2s；连续失败 5 次终止循环。存券成功后计一轮并开下一轮）。

---

## 17. 卡图 / 传送落地：清路径再官方点任务 ✅ 已实现

以后碰到类似问题（过图卡死、传送落地、官方导航往出发城镇走）默认这一套，**不要发明中间跳**：

1. `AbortEscortTaskPathFully`：先把 `m_IsStartingTaskPath=false`，再 Cancel + StopMove，并清 TaskManager 路径缓存、`AutoWarpIndex=0`、`TargetPoint`、`currentExecuting`
2. 等约 **2 秒**（等待期间每 tick 继续 abort，防止官方续上旧路）
3. `ClickEscortTaskLikeMouse`：侧栏 `m_Info` 上 `AutoWarpIndex=0` + `TaskManager.RunTask`

只 `CancelTaskPathfinding` 不够：不清 `AutoWarpIndex` / `TargetPoint`；`m_IsStartingTaskPath` 为 true 时 Cancel 直接 return。`movePoint` 是倒序表（index 0 = 目的地，表尾 = 出发城镇），缓存还在时再 `RunTask` 会 fallback 一路退回芙蕾雅/法兰。

**不要** `GeneralPointMoveTo` 自己走下一张/终点。普通卡楼梯仍是：静止 → 挪 1 格 → `ClickEscortTaskNav`。

参考：`SeqChapterTestUi.cs::AbortEscortTaskPathFully`、`_escort119OfficialResumePending`、`ClickEscortTaskLikeMouse`；规则 `seqchapter-official-nav-reset`。

---

