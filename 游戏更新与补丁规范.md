# 魔力宝贝：序章 — 游戏更新与补丁规范

> **唯一权威**：日常「客户端更新 / 打补丁 / 发傻瓜包」按本文执行。  
> Agent 入口摘要：仓库根 `AGENTS.md`；开发细节：`客户端热补丁开发指南.md`；Cursor 规则：`.cursor/rules/`。  
> 旧文档 `魔力宝贝序章补丁/客户端更新规则.md`、`魔力宝贝序章补丁/补丁维护.md` 已废弃，仅保留跳转。

---

## 1. 目录约定

| 角色 | 路径 | 规则 |
|------|------|------|
| **主开发 / 日常游戏 / 打补丁** | `E:\cross\魔力宝贝：序章` | 可打补丁；Agent 默认照顾此目录 |
| **干净官方底稿（只读）** | `E:\crosscopy\魔力宝贝：序章` | **永远不打补丁、不写入**；只跑官方 `Update.exe` |
| **发布产物** | `E:\cross\发布plugin\` | 傻瓜补丁 zip 输出目录 |
| **桥接实验（暂搁）** | `E:\cross桥接\魔力宝贝：序章` | 除非用户点名，否则不主动改 |

路径可用环境变量覆盖：`CROSS_ROOT`、`CROSSCOPY_ROOT`。

### 为什么要有 crosscopy

官方更新器在**已打补丁**的客户端上跑会互相干扰。因此：

1. 平时在 **cross** 玩（带补丁）。
2. 有更新时**只在 crosscopy** 跑官方更新器。
3. 再把官方文件同步回 cross，从干净 hotfix 重打补丁。

---

## 2. 一句话命令（首选）

入口脚本：`python tools/workflow.py <cmd>`（封装 `cross_update.py` / `publish_foolproof.py` 等）。

| 场景 | 命令 |
|------|------|
| 看状态 | `python tools/workflow.py status` |
| crosscopy 已更新 → 一条龙 | 先 `update --dry-run`；确认后 `update`；反外挂误报加 `--confirm-anticheat` |
| 只重打默认组合 | `python tools/workflow.py repatch`（**需关游戏**） |
| 发傻瓜补丁 | `python tools/workflow.py publish-foolproof` |
| 按清单发布 | `python tools/workflow.py publish-all` |

底层拆分（排查用）：`tools/cross_update.py status|diff|sync|anti-cheat|auto-update|restore`。

---

## 3. 客户端更新标准流程

### 3.1 用户侧

1. **关掉**所有 `cg37.exe`（Agent **不得擅自杀进程**，除非用户明确同意）。
2. 在 **crosscopy** 运行官方 `Update.exe`，等更新完成（crosscopy 保持纯净）。
3. 通知 Agent / 自己执行一条龙。

### 3.2 一条龙（推荐）

```bash
python tools/workflow.py update --dry-run              # 只读：反外挂探测
python tools/workflow.py update                        # 同步 + 换底稿 + 默认组合重打
# 若 anti-cheat 因二进制噪声误报拦截：
python tools/workflow.py update --confirm-anticheat
```

`auto-update` 会：

1. 对比旧 `.orig` vs crosscopy 新 `hotfix`（反外挂深度分析）。
2. 从 crosscopy **同步**官方新增/修改到 cross（含 `GameAssembly.dll`、`hotfix.dll.bytes` 等）；清理旧 `install\` 缓存。
3. 用 crosscopy 干净 hotfix 写成新底稿（`.orig` / `neworig`），更新 `EXPECTED_SIZE` / `tools/hotfix_baseline.json`（体积变了还会重建引擎）。
4. 从干净 `.orig` 按 **`DEFAULT_COMBO_KWARGS`** 重打默认组合。

### 3.3 仅重打（底稿已是新版）

```bash
python tools/workflow.py repatch
```

### 3.4 反外挂核对要点

diff / anti-cheat 报告重点看：

| 项 | 含义 |
|----|------|
| `GameAssembly.dll` | IL2CPP 原生层，常随版本变 |
| `hotfix.dll.bytes` 体积/哈希 | 变了必须换底稿重打 |
| `Plugins\x86_64\` 新 dll | 警惕新反外挂组件 |
| `global-metadata.dat` | 原生符号变化 |
| hotfix 内「新增安全字符串」 | 可能是二进制噪声（如碰巧像 `Gm`）；人工看是否有真正的上报/检测关键词 |

报告目录：`tools/update_reports/`。

---

## 4. 铁律

1. **永不污染 crosscopy**（只读对照；任何 patcher / `apply_combo` 不得以 crosscopy 内文件为输出目标）。  
   注意：部分 patcher 的 `--detect` **仍会写入**，禁止对 crosscopy 跑这类命令。
2. **Agent 默认不直接写** 游戏内 `hotfix.dll.bytes`；用户可用 GUI / 傻瓜包；`workflow update|repatch` 视为用户允许的固化代打。
3. **不杀 `cg37`**，除非用户明确同意（可列出 PID 再确认）。
4. hotfix **文件字节数不变**（`HotfixSize.Expected` / `EXPECTED_SIZE`）；禁止随意 Cecil 整包写出当最终交付。
5. 新增补丁：改 `tools/hotfix_patcher/` → 同步 `魔力宝贝序章补丁/scripts/` → 重建引擎 → **仅对副本 / `.orig` 自测** → 交付用户打。
6. 协议/反射：先查 `tools/常用反射方法速查.md`，再嗅探 `tools/hotfix_ilspy/`。
7. 废弃模块见 `tools/DEPRECATED.md`（九动等默认不扩展、发布文案不提）。

---

## 5. 默认补丁组合（`DEFAULT_COMBO_KWARGS`）

权威定义：`魔力宝贝序章补丁/scripts/patch_defaults.py`。

### 5.1 默认开（摘要）

| 类 | 内容 |
|----|------|
| 安全 | `kill_timescale_report=True`（**即使加速关也掐断** `CheckTimeScaleWarning` / `SendTimeScaleWarning`） |
| 面板 | `wiki_test_ui` 助手面板；烧卡/抓宠/无宠/卖银 DLL；计数挂机；采集提取；加点；上架；刷熊男 |
| 入口 | 客服→`autoskill`；分享→日常 + 礼包码；切后台/老板键限帧 30FPS |
| 多开 | `inject_bridge=True`（精简桥接）；`dragon_loop_ui=False`（龙族护航已卸载，打补丁删标记） |
| 其它 | 战斗长按详情；遇敌一级含哥布林/蝙蝠 |

### 5.2 默认关（加速类）

| 项 | 说明 |
|----|------|
| VIP 战斗倍速 | `vip=False`；需要时再开 |
| 技能特效加速 | **归属战斗倍速**：默认关；`vip=True` 时 `apply_combo` **自动带上**（默认 **3x**） |
| 地图跑速 | `map_sprint=False` |
| 战斗加速方案2 | `combat_accel=False`（跑位/箭矢/击飞/去慢放） |
| 过场加速 | `transition_speed=False` |
| 九动 | 永久封存，`battle_nine_*=False` |

临时开加速示例（不改默认文件）：`vip=True, vip_non_vip=True, vip_scale=3`（特效会自动 3x）。

### 5.3 倍速上报拦截

- 独立 patcher：`kill-timescale-report-patch`；与 Vip / CombatAccel 自带逻辑幂等互跳。
- **默认组合总是带**；不要默认关掉。

---

## 6. 玩家打补丁方式

### 6.1 开发 GUI

1. 关游戏 → `魔力宝贝序章补丁\启动补丁GUI.bat`
2. 选游戏目录 → 初始化 / 应用补丁  
3. 重启游戏验证

### 6.2 傻瓜补丁

```bash
python tools/workflow.py publish-foolproof
```

一次产出（说明**不提**九动）：

| 包 | 路径约定 |
|----|----------|
| 融合版 | `E:\cross\发布plugin\傻瓜补丁_融合版_*.zip`（龙族护航已卸载） |

包内含：补丁 GUI、`HotfixPatcher`、外置 DLL 源码、**多开器**、**窗口监视** 等。

助手面板含**七夕 #119 月宫救兔循环**（阿凯/哥拉尔；存兑换券后计一轮）；临时活动，**等用户明确下令后再从面板源码永久删除**。

玩家：关游戏 → 解压 → `一键打补丁.bat` → 勾选「战斗加速 / 加速2 / 跳帧 / 多开器适配」→ 一键打补丁。

- **战斗加速开**：VIP 倍速 + **特效 3x** + 心跳回传 1.5x（连带 kill-report）。
- **战斗加速关**：无 VIP / 无特效加速；kill-report 仍可由默认组合携带。

---

## 7. 工具链职责

| 路径 | 作用 |
|------|------|
| `tools/workflow.py` | 统一入口 |
| `tools/cross_update.py` | cross ↔ crosscopy 状态/对比/同步/一条龙 |
| `tools/hotfix_patcher/` | C# 补丁引擎源码 → 发布为 `HotfixPatcher.exe` |
| `魔力宝贝序章补丁/scripts/` | GUI、`apply_combo_patch.py`、`patch_defaults.py`、`publish_foolproof.py` |
| `tools/seqchapter_*/` | 外置玩法 DLL 源码 |
| `tools/hotfix_ilspy/` | 反编译参考（只读） |
| `tools/hotfix_baseline.json` | 干净 hotfix 基线哈希/体积 |

游戏内写入目标（由补丁流程部署，非手改）：

- `cg37_Data/assets/hotfixdata/hotfix.dll.bytes`（+ `.orig`）
- `SeqChapter*.dll.bytes` 等外置 DLL

---

## 8. 体积与余量（更新后）

1. 干净 hotfix 字节数 → 对齐 `HotfixSize.Expected` / `patch_common.EXPECTED_SIZE`（一条龙通常自动做）。
2. `HotfixPatcher slack-report --hotfix <.orig>`：`.text` VA 间隙 / raw slack。
3. **原地改写**类（VIP、Sprint、长按、客服缩体）余量为 0 通常仍可打。  
   **追加 IL**类（特效加速约 +49B、桥接加载器等）依赖余量；不够则关对应项或改方案。

历史体积备忘（节选）：早期约 6.8M～7.0M；2026-08 起常见 **7,173,632**（以 `hotfix_baseline.json` / 当前 `.orig` 为准）。

---

## 9. 相关文档索引

| 文档 | 用途 |
|------|------|
| `AGENTS.md` | Agent 少思考入口 |
| `客户端热补丁开发指南.md` | IL/引擎开发细节 |
| `tools/常用反射方法速查.md` | 协议与反射片段 |
| `tools/DEPRECATED.md` | 封存模块 |
| `.cursor/rules/seqchapter-*.mdc` | Cursor 强制规则 |

---

*文档版本与仓库同步；有流程变更时只改本文件，勿再分叉多份「更新规则」。*
